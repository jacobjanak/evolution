define(function() {

  const config = {
    tileSize: 100,
    plantSize: 20,
    animalSize: 10,
    worldDimensions: {
      height: 6,
      width: 10
    },
    spawnCount: {
      plants: 50,
      herbivores: 20,
      carnivores: 10
    }
  };

  return config;
})
