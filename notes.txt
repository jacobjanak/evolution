refactor a lot of stuff
> config doesn't need to be its own file
> if the user shrinks the world, destroy creatures that are outside of the new world boundaries
> create a class called Organism
> find.js could really just be static methods of Organism
add kills, offspring, and age to the animals
make more traits subject to mutation
