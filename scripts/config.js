define(function() {

  const config = {
    tileSize: 50,
    plantSize: 5,
    worldDimensions: {
      height: 10,
      width: 10
    },
    spawnCount: {
      plants: 50,
      herbivores: 20,
      carnivores: 10
    }
  };

  return config;
})
